export function Header() {
  return (
    <header className="flex items-center justify-between">
      <h1 className="text-2xl font-semibold">Personal Expense Agents</h1>
      <span className="text-sm text-gray-500">Local-first ? Private by default</span>
    </header>
  );
}
